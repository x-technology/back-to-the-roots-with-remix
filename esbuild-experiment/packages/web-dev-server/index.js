#!/usr/bin/env node
const { search, getURL } = require("@esbuild-experiment/dev-server");
const { parse, relative, join } = require("node:path");

(async () => {
  console.log("start web dev server")
})();